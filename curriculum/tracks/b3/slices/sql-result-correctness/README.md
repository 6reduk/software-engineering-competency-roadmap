---
id: b3.index.sql-result-correctness
kind: index
title: "Корректность результата SQL: маршрут SQ01"
owner_track: b3
module: b3.module.derive-correct-query-results
coverage:
  - target: b3.capability.compose-relational-queries
    role: reference
  - target: b3.capability.derive-grouped-window-results
    role: reference
status: accepted
updated: 2026-09-11
language: ru
last_verified: 2026-09-09
versions:
  PostgreSQL: "18.6"
  psql: "18.6"
---

# Когда добавление платежей удваивает стоимость заказа

Финансовая команда использует Orders report для сверки стоимости заказов и поступивших денег. После добавления платежей в запрос стоимость заказа 1 выросла с 200 до 400, хотя новых покупок не было. Цель маршрута — проследить, какие строки складываются, и получить одну проверяемую строку на заказ с остатком и рангом.

## Вход и маршрут

Достаточно уметь читать таблицу и первичный/внешний ключ, запускать SELECT, понимать псевдоним столбца и простое условие. Минимальную SQL-среду участник создаёт сам; сервер и psql 18.6 — проверенная пара. Python нужен только при выборе Python-оснастки. Эксплуатационный доступ к выделенной базе предоставляется по месту выполнения; API, ORM и настройка платформы не входят в задание.

Сначала изучается [составление запросов с проверяемой семантикой](../../level-outcomes.md#compose-relational-queries), затем [агрегаты и окна без искажения данных](../../level-outcomes.md#derive-grouped-window-results). Это две capabilities `b3.capability.compose-relational-queries` и `b3.capability.derive-grouped-window-results` из [модуля корректных результатов](../../modules.md#sql-querying). [Обязательная зависимость](../../prerequisites.md#зависимости-полного-batch) направлена от grouped/window к compose: сначала нужен правильный набор строк, затем вычисления над ним. [Внешняя подготовка](../../prerequisites.md#внешняя-подготовка-и-границы) зависит от выбранной оснастки. Сценарий — [SQ01](../../scenarios.md#sq01).

Пять материалов пакета, включая эту точку входа:

| Материал | Для чего открыть |
|---|---|
| [Индекс](README.md) | Выбрать маршрут и понять общий результат |
| [Инженерная заметка](learn/join-cardinality-and-result-grain.md) | Понять механизм ошибки и сравнить классы решений |
| [Интервью](interview/correct-query-wrong-total.md) | Проверить рассуждение от L1 до L3 |
| [Kata](kata/stop-join-multiplication.md) | Самостоятельно исправить запрос по заданным данным и правилам |
| [Проект](project-spec/orders-financial-summary.md) | Защитить неоднозначную семантику и расширить проверки в своём репозитории |

## Общий результат

Канонические данные: `orders = {1,2,3,4}`; строки `(line_id, order_id, amount)` — `(101,1,100)`, `(102,1,100)`, `(103,2,50)`, `(104,4,200)`; платежи `(payment_id, order_id, amount)` — `(201,1,60)`, `(202,1,40)`, `(203,4,200)`. Две строки по 100 — разные покупки, а не дубликат записи. Заказ 2 не оплачен, заказ 3 пуст, заказ 4 полностью оплачен и равен заказу 1 по стоимости.

Договор сводки: одна строка на каждый заказ, включая пустой. `line_count` и `payment_count` считают реальные дочерние записи; `line_total` и `paid_total` складывают их суммы. Все деньги — целые minor units (минимальные денежные единицы одной валюты). Отсутствие строк или платежей означает нулевые число и сумму; это локальное правило отчёта, а не общее равенство `NULL = 0`. Исходные суммы обязательны (`NOT NULL`): неизвестная сумма платежа не допускается. `NULL` в этом наборе возникает из отсутствующего совпадения при outer join.

`balance = line_total - paid_total`: положительное значение — недоплата, отрицательное — переплата; нулевой остаток пустого заказа не означает состоявшуюся оплату. `total_rank` ранжирует по `line_total` по убыванию без пропусков между различными суммами: равным суммам — одинаковый ранг. Выдача отдельно упорядочена по `line_total DESC, order_id ASC`; ID не входит в смысл равенства рангов.

Ожидаемые строки в порядке выдачи:

| order_id | line_count | line_total | payment_count | paid_total | balance | total_rank |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 2 | 200 | 2 | 100 | 100 | 1 |
| 4 | 1 | 200 | 1 | 200 | 0 | 1 |
| 2 | 1 | 50 | 0 | 0 | 50 | 2 |
| 3 | 0 | 0 | 0 | 0 | 0 | 3 |

Прямое соединение двух строк и двух платежей создаёт четыре пары. Поэтому правильная итоговая форма сама по себе ещё ничего не доказывает: GROUP BY может снова дать одну строку, сохранив завышенную сумму. Проверять нужно весь путь от исходных записей до итоговой таблицы.

Гранулярность (grain) — смысл одной строки; кратность соединения — число совпадений на исходную строку; peers — строки с равным значением оконного порядка. В этом маршруте окно ранжирует уже полученные строки заказов и не устраняет размножение записей.

Пять материалов и [coverage-регистрация](../../coverage-map.md) приняты после G24. Назначение задания не доказывает его выполнение или proficiency; результат участника потребует отдельной проверки. Kata ограничена L1/L2, самостоятельное решение неоднозначности вынесено в проект L2/L3.

## Пределы

Здесь проверяется смысл operational SQL. [IO01](../../scenarios.md#io01) отвечает за планы и индексы, [RM01](../../scenarios.md#rm01) — за общее проектирование схемы; аналитические pipelines принадлежат [B4](../../../catalog/b4-data-engineering.md). ORM/API, изоляция транзакций, SE01, BR01, L4/L5 и module completion в этот пакет не входят. Скорость и production readiness не следуют из правильной таблицы.

## Источники и актуальность

Проверено 2026-09-09 на PostgreSQL/psql 18.6. [Таблица поддержки](https://www.postgresql.org/support/versioning/) подтверждает текущую stable-ветку 18 и minor 18.6 на эту дату; это воспроизводимый учебный baseline, а не автоматический выбор production-версии.

- [Соединения, фильтры и группировка](https://www.postgresql.org/docs/18/queries-table-expressions.html) задают состав промежуточных строк.
- [Aggregate expressions](https://www.postgresql.org/docs/18/sql-expressions.html#SYNTAX-AGGREGATES) различает COUNT(*)/COUNT(column) и DISTINCT над значениями; [aggregate functions](https://www.postgresql.org/docs/18/functions-aggregate.html) описывает NULL у SUM пустого входа.
- [Сравнения с NULL](https://www.postgresql.org/docs/18/functions-comparison.html) объясняют неизвестный результат предиката.
- [Оконные функции](https://www.postgresql.org/docs/18/functions-window.html) определяют равные ранги peers, а [ORDER BY](https://www.postgresql.org/docs/18/queries-order.html) — отдельный порядок выдачи.
- [Оконные функции в запросе](https://www.postgresql.org/docs/18/tutorial-window.html): оконная функция выполняет вычисление по связанным строкам, не сворачивая их в одну строку, и видит виртуальную таблицу после `WHERE`, `GROUP BY` и `HAVING`.

Правила о составе строк и вычислениях выше — семантика PostgreSQL. Сохранение всех заказов, нулевая сумма отсутствующих платежей и ранги без пропусков — локальные решения этого отчёта. Выбор раздельных этапов — инженерный вывод из кратности; превосходство по скорости не заявлено.
