---
id: ela.work-package.m1-8-14a-delivery-plan-findings-correction
kind: work-package
status: accepted
owner: autonomous-planning-agent
updated: 2026-09-06
language: ru
---

# M1.8.14a — локальное закрытие findings G13 delivery plan

## Objective

Закрыть C-1 и C-2 из решения оркестратора двумя локальными правками в `curriculum/tracks/b2/module-delivery-plan.md`. Не пересчитывать и не переписывать план целиком; не менять очередь и выбор Safe Service Evolution/S11. Повторный полный G13 не требуется.

## Required context

Полностью прочитать:

1. настоящий work package;
2. `authoring/workflow/content-author-workflow.md`;
3. `governance/reviews/M1.8.14-G13-b2-delivery-plan-resolution.md`;
4. `governance/reviews/M1.8.14-G13-b2-delivery-plan-consolidated.md`;
5. `governance/work-packages/M1.8.14-b2-delivery-plan-refresh.md`;
6. `curriculum/tracks/b2/module-delivery-plan.md`;
7. Delivery и Evolution outcomes в `curriculum/tracks/b2/level-outcomes.md`;
8. Transactional Persistence membership в `curriculum/tracks/b2/modules.md`.

## Ownership

Разрешено изменить только:

- `curriculum/tracks/b2/module-delivery-plan.md`.

Запрещено изменять registry, coverage audit, semantic IDs, counts, module membership/states, DAG, очередь кандидатов, catalogs, coverage map, scenarios, standards, reviews, decisions, work packages и program status. Front matter плана сохраняет `status: review`; разрешено обновить `updated` на `2026-09-06`.

## Required changes

### C-1 — область истинности аргумента о новом outcome

В сравнении кандидатов заменить глобально звучащую формулировку «единственный кандидат с новым достижимым outcome» на точную: Safe Service Evolution/S11 — единственный **из сравниваемых пяти кандидатов**, который даёт новый локально достижимый outcome.

Рядом кратко зафиксировать, что Evolution L1 (`b2.level-outcome.evolve-api-contracts-l1-apply-additive-change`) также остаётся глобально непокрытым и локально достижимым, но отдельный L1-only package сейчас отклонён: он повторял бы принятый S03 flow вместо открытия собственного flow `not-started` модуля.

Не добавлять шестого кандидата в таблицу и не менять порядок очереди.

### C-2 — полный перечень пробелов Transactional Persistence

В колонке «Главный пробел» строки Transactional Persistence Integration перечислить все три непокрытых membership outcomes:

- Cache L4;
- Idempotency L4;
- Verification L4;

и отсутствие module-gate decision.

Не менять membership `13/10`, состояние `partial` и строки outcome audit.

## Carried notes — не исправлять в этом пакете

C-3–C-6 не являются правками текущего плана. Они уже приняты как обязательный вход будущего S11 authoring package:

- актуализировать метку M1.8.13 в `coverage-map.md` до `accepted`;
- зарегистрировать kata-role для Delivery capability;
- объяснить ценность собственного S11 Delivery L3 при уже существующем supporting evidence C5;
- ограничить Gate 0 коротким decision/readiness record, а не строительством платформы.

Не изменять `coverage-map.md` и не создавать S11 authoring package в этом проходе.

## Non-goals

- повторный coverage/module/DAG audit;
- изменение выбора S11 или сравнения кандидатов по существу;
- проектирование rollout substrate, cohorts, thresholds или recovery paths;
- создание Gate 0, учебных материалов или ImplementationReference;
- добавление Delivery/Evolution/Compatibility/Verification/B6/B7 coverage;
- редактирование review-отчётов, catalogs или program status.

## Acceptance criteria

- [ ] C-1 и C-2 закрыты отдельными видимыми правками.
- [ ] Область сравнения ограничена пятью кандидатами, а Evolution L1 debt не скрыт.
- [ ] Строка Transactional Persistence перечисляет Cache L4, Idempotency L4 и Verification L4.
- [ ] Сохранены `47 / 227 / 19+208 / 57/12 / 7 probe-only`, 47 registry rows и 69 outcome rows.
- [ ] Сохранены membership `13/10`, states `0/8/2/0`, internal hard DAG, очередь и выбор S11.
- [ ] План остаётся `status: review`; `updated` не позднее 2026-09-06.
- [ ] Относительные ссылки, fences, `<details>`, EOF/newline, conflict markers и trailing whitespace проверены.
- [ ] Файлы вне Ownership не изменены.

## Handoff

Передать оркестратору:

1. mapping C-1/C-2 к точным строкам и новым формулировкам;
2. подтверждение сохранения числовых и semantic invariants;
3. подтверждение неизменности очереди, S11 choice и hard DAG;
4. результаты механических проверок;
5. открытые вопросы либо явное указание, что их нет.
