---
id: ela.work-package.m2-1a-g29-findings-correction
kind: work-package
status: accepted
owner: autonomous-content-author
updated: 2026-09-17
language: ru
---

# M2.1a — закрытие обязательного finding G29

## Objective

Закрыть C-1 одним механическим изменением metadata B5 boundary intake, не меняя его
содержание и фазовое решение.

## Ownership

Разрешено изменить ровно один файл:

- `governance/planning/intakes/b5-boundary-intake.md`.

Новые файлы не создавать. Selection, reviews, work packages, tracks, routes,
program governance, B2/B3 и будущий B5 blueprint не изменять.

## Context manifest

1. `governance/reviews/M2.1-G29-b5-boundary-intake-resolution.md`;
2. `governance/reviews/M2.1-G29-b5-boundary-intake-consolidated.md`;
3. `governance/planning/intakes/b5-boundary-intake.md`;
4. `governance/architecture/metamodel/metadata.md`;
5. соседние файлы `governance/planning/intakes/*.md` только для проверки конвенции и
   уникальности ID.

## Required change

В front matter добавить ровно одно поле:

```yaml
id: b5.plan.boundary-intake
```

Поле разместить перед `kind`. Обновить `updated` на `2026-09-17`. Сохранить
`kind: plan`, `owner_track: b5`, `status: review`, `language: ru` и заголовок.

Проверить, что `b5.plan.boundary-intake` встречается как определение `id` ровно один
раз во всём workspace и не конфликтует с существующими semantic IDs. Это metadata
planning artifact, а не новый capability/outcome/coverage target.

## Explicit non-change

- Не менять тело intake.
- Не исправлять устаревшую lifecycle-фразу selection: это действие оркестратора.
- Не реализовывать C-2–C-4: они относятся к будущему breadth-pass.
- Не создавать B5 blueprint, modules, capabilities, outcomes, scenarios, role links
  или coverage.
- Не выполнять Gate 0, runtime experiment, выбор deep slice или BR01.
- Не запускать повторный G29 и не объявлять acceptance.

## Acceptance checks

- Изменён ровно один Ownership-файл.
- В front matter есть ровно один `id: b5.plan.boundary-intake`.
- ID уникален в workspace.
- Дата — `2026-09-17`; остальные metadata и всё тело intake сохранены.
- YAML, относительные ссылки, Markdown, UTF-8, EOF, trailing whitespace и conflict
  markers проверены.

## Handoff

Передать точный diff front matter, результат проверки уникальности ID, подтверждение
неизменности тела и механические проверки. Автор не выполняет review, acceptance,
исправление selection или следующий breadth-pass.
