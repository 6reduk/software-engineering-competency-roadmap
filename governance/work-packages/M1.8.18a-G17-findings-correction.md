---
id: ela.work-package.m1-8-18a-g17-findings-correction
kind: work-package
status: accepted
owner: autonomous-planning-agent
updated: 2026-09-08
language: ru
---

# M1.8.18a — закрытие findings G17

## Objective

Точечно закрыть A-M-1, A-N-1 и A-N-2 из решения G17 в `curriculum/tracks/b2/module-delivery-plan.md`, не пересчитывая и не переписывая принятый delivery plan целиком. Сохранить фазовое решение, coverage snapshot, module states и hard DAG.

## Ownership

Агент может изменить только:

- `curriculum/tracks/b2/module-delivery-plan.md`.

Запрещено изменять учебные материалы, coverage map, каталоги, prerequisites, scenarios, role routes, standards, work packages, reviews, decisions и program status.

## Context manifest

Полностью прочитать:

1. настоящий work package;
2. `authoring/workflow/content-author-workflow.md`;
3. `governance/reviews/M1.8.18-G17-b2-breadth-pass-glm.md`;
4. `governance/reviews/M1.8.18-G17-b2-breadth-pass-resolution.md`;
5. `governance/reviews/M1.8.18-orchestrator-intake.md`;
6. `curriculum/tracks/b2/module-delivery-plan.md`;
7. строки `control-concurrency-cancellation` и `manage-service-resource-lifecycle` в `curriculum/tracks/b2/capabilities.md` и `curriculum/tracks/b2/prerequisites.md`.

Профиль B не выполнялся. Не создавать отсутствующий review, consolidated, acceptance или новый planning/content package.

## Required changes

### C-1 — primary/supporting split External Service Integration

В строке External Service Integration таблицы десяти модулей:

- перенести `control-concurrency-cancellation` в primary-часть до `;` рядом с `integrate-external-services`;
- оставить `control-service-load-resources` в supporting-части;
- в prerequisite-колонке явно указать, что external integration, cancellation и load control требуют lifecycle;
- не менять canonical membership `11/10`, module state, coverage или DAG.

### C-2 — доказательная сила SHA-256

В разделе воспроизводимости:

- сохранить фактический авторский digest и утверждение о выполненном сравнении до/после;
- назвать это авторской scope verification текущего refresh;
- явно указать, что без отдельно сохранённого независимого pre-snapshot reviewer не может повторно доказать состояние «до» только по текущему digest;
- не ослаблять независимо повторяемые YAML coverage, registry, outcomes, memberships и DAG checks.

### C-3 — E0–E4 как дельта artifact registry

Сделать блок E0–E4 корректно рендерящейся таблицей:

- предпочтительно добавить человеческий подзаголовок «Дельта M1.8.17» и повторить header/separator columns перед E0;
- альтернативно объединить E0–E4 с основной таблицей без потери строк;
- сохранить коды, ссылки, kinds, module ownership и role counts побуквенно по смыслу.

## Explicit non-changes

- Не менять числа `57 / 249 / 21+228 / 58+11 / 7` и role totals.
- Не менять 69 outcome rows, artifact registry content, memberships и states `0/10/0/0`.
- Не менять hard DAG, external readiness или accepted coverage.
- Не менять определение breadth-pass, HTTP Runtime debt или L4 adoption boundary.
- Не менять выбор consolidation, его no-new-coverage scope и exit trigger.
- Не создавать consolidation artifacts.
- Не переводить plan в `accepted` и не менять program/module gates.

## Acceptance criteria

- [ ] C-1–C-3 имеют отдельные видимые исправления.
- [ ] External primary/supporting split совпадает с каталогом capabilities.
- [ ] Prerequisite statement не меняет набор из 13 hard edges.
- [ ] SHA-256 claim не выдаётся за независимо воспроизводимое доказательство прошлого состояния.
- [ ] E0–E4 рендерятся как таблица с header row.
- [ ] Все числовые, coverage, state и phase-decision инварианты сохранены.
- [ ] `module-delivery-plan.md` остаётся `status: review`, `updated: 2026-09-08`.
- [ ] YAML, ссылки, fences, раскрываемые блоки, whitespace и conflict markers проверены.
- [ ] Файлы вне Ownership не изменены.

## Handoff

Передать оркестратору:

1. mapping C-1–C-3 к точным строкам;
2. подтверждение primary/supporting и prerequisite semantics;
3. итоговую формулировку scope verification;
4. способ оформления E0–E4;
5. сохранённые coverage/state/DAG/phase инварианты;
6. результаты механических проверок;
7. открытые вопросы и рекомендуемый scope точечного verification-pass.

Агент не выполняет повторный coverage review, не принимает G17 и не создаёт следующий package.
