---
id: ela.review.m1-6-findings-closure-glm
kind: review
title: M1.6 — независимый review закрытия findings C-1–C-11 (GLM)
status: final
reviewer: glm
reviewer_model: glm-5.3
updated: 2026-08-15
language: ru
scope: curriculum/tracks/b2/slices/evolvable-api-contracts/ + curriculum/tracks/b2/coverage-map.md (строка C-9)
---

# M1.6 — review закрытия findings C-1–C-11 (GLM)

## Scope и метод

Проверено закрытие findings C-1–C-11 из [`M1.5-technical-repass-consolidated.md`](M1.5-technical-repass-consolidated.md) по правкам, выполненным в рамках [`governance/work-packages/M1.6-close-technical-repass-findings.md`](../work-packages/M1.6-close-technical-repass-findings.md). Дата проверки: 2026-08-15.

Прочитаны полностью: шесть материалов слайса (текущие версии), `authoring/workflow/review-protocol.md`, оба work package M1.6, `curriculum/tracks/b2/coverage-map.md`, `governance/architecture/metamodel/metadata.md`, `authoring/standards/editorial-standard.md`. Review другого агента не читался до завершения анализа.

Метод:

- каждый изменённый файл сверен с его до-M1.6 версией (полные копии зафиксированы в контексте reviewer при M1.5 re-pass);
- механические проверки: `grep` по `c-17`, `"sku"`, `book-1`, `docs.pydantic.dev`, `L1–L4 follow-ups` в пределах scope — устаревших вариантов не найдено; conflict markers и trailing whitespace отсутствуют;
- структурная целостность: ID, `owner_track`, `module`, coverage targets/roles, `status: review` во всех шести файлах не изменились; Mermaid-блоки (7) и `<details>` не затронуты;
- ограничение метода: git-метаданные репозитория недоступны (`.git` отсутствует), поэтому «diff ограничен Ownership» подтверждается сопоставлением полных текстов, а не VCS-diff; это же подтвердил orchestrator intake в work package.

**Проверяемые материалы не редактировались**; единственный созданный файл — настоящий review-артефакт.

## Итоговая таблица C-1–C-11

| Finding | Статус | Evidence (кратко) |
|---|---|---|
| C-1 RFC 9110/9111 | **closed** | `learn/http-contract-execution-boundary.md:180` и `learn/compatible-api-change.md:189` разводят: RFC 9110 §12.5.5 — смысл `Vary`, RFC 9111 §4.1 — поведение кэша; RFC 9111 добавлен в источники обоих brief |
| C-2 единая история | **closed** | kata:47-49 и project-spec:75-76 теперь `customer_id: "c-42"`, `product_id: "p-7"`; устаревших `c-17`/`sku` в scope нет |
| C-3 freshness metadata | **closed** | `last_verified: 2026-08-15` + `versions` (включая `pydantic_validation_semantics: "2.x"`) добавлены в interview, kata, project-spec; overrides двух learn сохранены и расширены; соответствует `governance/architecture/metamodel/metadata.md` (artifact override Module baseline). См. note N-3 про README |
| C-4 Pydantic URLs | **closed** | Оба brief: `pydantic.dev/docs/validation/latest/concepts/{fields,serialization}/`; конечные страницы проверены — существуют и каноничны |
| C-5 кэшируемость POST | **closed** | project-spec:180-182 — сценарий 8 оговорен «явно кэшируемого ответа», условие вынесено к RFC 9110 §9.3.3, предложен вариант `GET /orders/{id}`; «общий кэш не сохраняет произвольный ответ POST по умолчанию» — дословно соответствует §9.3.3 |
| C-6 сокращённый validation response | **closed** | http-boundary:33-45 — пример предварён «Ниже показан сокращённый вариант…» и дополнен `"msg": "Field required"` |
| C-7 терминология | **closed** | interview:46 и project-spec:59 — абзац с русским объяснением `consumer`/`producer` и ссылкой на `API caller`/`API provider` brief (валидный относительный путь и якорь заголовка); словари обоих документов дополнены пометкой «в brief — `API caller`/`API provider`» |
| C-8 JSON Pointer | **closed** | http-boundary:156 — `#/delivery/window/end`, fragment-форма как в примере RFC 9457 §3 |
| C-9 coverage-map | **closed** | coverage-map:41 — «L2–L4 follow-ups»; строка 54 (таблица targets) совпадает с фактическим coverage interview; остальные строки не тронуты |
| C-10 response model | **closed** | http-boundary:196 — утвердительная формулировка «сгенерированная JSON Schema продолжает описывать полную модель» |
| C-11 default scope | **closed** | http-boundary:104 — «область по умолчанию — `request`: завершающий код выполняется после отправки ответа; явно выбранная `scope="function"` — после возврата операции, но до отправки»; добавлена ссылка на документацию FastAPI |

## Перепроверенные внешние утверждения (primary sources)

| Утверждение в правках | Первичный источник | Результат |
|---|---|---|
| RFC 9110 §9.3.3: ответы POST кэшируются только при явной информации свежести | rfc-editor.org/rfc/rfc9110.html §9.3.3 (полный текст) | подтверждено дословно: «Responses to POST requests are only cacheable when they include explicit freshness information… and a Content-Location header field that has the same value as the POST's target URI» |
| RFC 9110 §12.5.5 определяет `Vary` (перечисление заголовков, повлиявших на выбор представления) | тот же, §12.5.5 | подтверждено |
| RFC 9111 §4.1: MUST NOT использовать сохранённый ответ без совпадения номинированных `Vary` заголовков | rfc-editor.org/rfc/rfc9111.html §4.1 (проверено в M1.5, не изменилось) | подтверждено |
| FastAPI: default scope для dependency с `yield` — `request`, cleanup после отправки ответа; `function` — до | fastapi.tiangolo.com/tutorial/dependencies/dependencies-with-yield/ (проверено в M1.5, не изменилось) | подтверждено |
| `response_model_include/exclude`: JSON Schema остаётся полной модели | fastapi.tiangolo.com/tutorial/response-model/ (проверено в M1.5, не изменилось) | подтверждено |
| Пример RFC 9457 использует fragment-форму pointer (`#/age`, `#/profile/color`) | rfc-editor.org/rfc/rfc9457.html §3 (проверено при консолидации M1.5) | подтверждено |
| Pydantic URL каноничны и доступны | pydantic.dev/docs/validation/latest/concepts/serialization/ (fields — через redirect в M1.5) | подтверждено |

## Regression-проверка

- **Метаданные/структура:** ID, `owner_track`, `module`, coverage (43 edges / 21 target), `status: review` — без изменений во всех шести файлах; README слайса не менялся вовсе.
- **Техническая корректность правок:** новых ошибок не найдено; обе «канонические» формулировки (RFC-сплит, POST-кэширование) сверены с полными текстами RFC.
- **Ссылки:** новые относительные ссылки interview→`../learn/compatible-api-change.md#какие-комбинации-версий-нужно-проверить` и project-spec→та же — пути существуют, якорь соответствует заголовку `## Какие комбинации версий нужно проверить`; в Obsidian и GitHub-слага совпадают.
- **Markdown/Mermaid/`<details>`:** fences сбалансированы, 7 диаграмм не изменены, `<details>` целы; conflict markers/trailing whitespace отсутствуют.
- **Единая история:** все примеры шести документов — одна семья (`c-42`/`product_id`/`quantity`); противоречий нет.
- **Readability (docs/09):** вставки локальны, связаны с окружающим текстом и следуют §4 стандарта (русское объяснение → английский термин в скобках); правки не превратились в справочник и не раскрывают решение kata/project. Регрессии не найдено.
- **Границы пакета:** изменение вне каталога слайса — только строка 41 coverage-map (C-9), что разрешено Ownership.

## Новые findings

Формат — `authoring/workflow/review-protocol.md`. Все три — note; ни один не мешает признать C-1–C-11 закрытыми.

```yaml
severity: note
artifact: curriculum/tracks/b2/slices/evolvable-api-contracts/project-spec/evolvable-orders-api.md
criterion: practice
finding: в изменённой формулировке сценария 8 согласование «два формата явно кэшируемого ответа, выбранных по заголовку» читается с натяжкой
evidence: project-spec:180 — числительное «два» с родительным ед. ч. «формата» и причастием мн. ч. «выбранных»; смысл понятен, но спотыкается при чтении вслух
recommendation: при следующей редакционной итерации сгладить, например «смешивает два формата ответа, выбранного по заголовку» или «…форматы ответа, выбранные по заголовку»
```

```yaml
severity: note
artifact: curriculum/tracks/b2/slices/evolvable-api-contracts/kata/compatible-delivery-contract-change.md, project-spec/evolvable-orders-api.md
criterion: freshness
finding: блок versions в interview, kata и project-spec — единообразная копия из learn, включая baselines (oas_3_1_baseline, fastapi_documented_output), которых тексты kata/project-spec не утверждают
evidence: frontmatter трёх файлов идентичен; work-package M1.6 требовал «осмысленные» baselines, а не копию
recommendation: допустимо оставить (метаданные истинны и согласованы); чище — сократить до фактически утверждаемых baselines или перенести общий якорь в module-level verified_baseline, как предусматривает governance/architecture/metamodel/metadata.md
```

```yaml
severity: note
artifact: curriculum/tracks/b2/slices/evolvable-api-contracts/README.md
criterion: freshness
finding: README содержит версионно-зависимые утверждения («опубликована OAS 3.2.0», «FastAPI… openapi: 3.1.0») без artifact-level last_verified/versions
evidence: README.md:79 (раздел «Актуальность технической базы»); consolidated C-3 помечал README как опцию («при желании»), поэтому closed-статус C-3 это не опровергает
recommendation: опционально добавить last_verified/versions в README либо зафиксировать решение, что prose-якорь (дата и версии указаны в тексте) достаточен
```

## Counts

| Severity | Count |
|---|---|
| blocker | 0 |
| major | 0 |
| minor | 0 |
| note | 3 |

## Verdict

**`accepted`.**

- C-1–C-11 — все **closed**; каждая правка видима в разрешённом файле, технически корректна и сверена с первичным источником.
- Новых blocker/major/minor нет; три note — необязательные улучшения, не создающие долга, требующего отложенного принятия.
- Регрессий (структура, ссылки, Mermaid, `<details>`, единая история, readability) не обнаружено; изменения не вышли за Ownership.

Reviewer не изменяет статусы учебных материалов и не закрывает M1.5/M1.6 от имени оркестратора. Подтверждение: проверяемые файлы не редактировались; создан только настоящий артефакт `governance/reviews/M1.6-findings-closure-glm.md`.
