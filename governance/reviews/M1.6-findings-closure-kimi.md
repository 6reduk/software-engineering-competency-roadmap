---
id: ela.review.m1-6-findings-closure-kimi
kind: review
title: M1.6 — независимый review закрытия findings C-1–C-11 (kimi)
status: accepted
updated: 2026-08-15
language: ru
reviewer: kimi
---

# M1.6 — review закрытия findings C-1–C-11

## Scope и дата проверки

Дата: 2026-08-15. Проверялось закрытие C-1–C-11 из `governance/reviews/M1.5-technical-repass-consolidated.md` по work packages `governance/work-packages/M1.6-close-technical-repass-findings.md` и `governance/work-packages/M1.6-review-findings-closure.md`.

Проверенные материалы (прочитаны полностью после правок): шесть документов `curriculum/tracks/b2/slices/evolvable-api-contracts/`, строка и таблица первого slice в `curriculum/tracks/b2/coverage-map.md`, freshness-правила `governance/architecture/metamodel/metadata.md`, `authoring/standards/editorial-standard.md` (только на предмет readability-регрессии), `authoring/workflow/review-protocol.md`.

**Проверяемые файлы не редактировались.** Создан только этот review-артефакт. Review других агентов по M1.6 до завершения анализа не читались.

## Итоговая таблица C-1–C-11

| Finding | Статус | Evidence |
|---|---|---|
| C-1 — источники кэширования | `closed` | `learn/http-contract-execution-boundary.md:180` и `learn/compatible-api-change.md:189` разводят: смысл `Vary` → RFC 9110 §12.5.5, поведение кэша при выборе сохранённого ответа → RFC 9111 §4.1. RFC 9111 добавлен в «Первичные источники» обоих briefs. Перепроверено по тексту RFC 9111 §4.1 ("the cache MUST NOT use that stored response without revalidation unless all the presented request header fields nominated by that Vary field value match…") — пересказ в `compatible-api-change.md:189` точен. |
| C-2 — единая сквозная история | `closed` | `kata/…:46-49` и `project-spec/…:75-76` переведены на `customer_id: "c-42"` и `product_id: "p-7"`; поиск по `c-17` и `"sku"` в `pilot/b2` не находит устаревших вариантов. Различие `quantity` (1 vs 2) и дат окна (08-18 vs 08-20) — вариации значений, а не сущностей. |
| C-3 — freshness metadata | `closed` | `last_verified: 2026-08-15` и `versions` добавлены в interview, kata, project-spec; overrides двух briefs сохранены и согласованы (одинаковые 4 ключа, включая новый `pydantic_validation_semantics: "2.x"` — behavior baseline, а не номер установленного пакета). Slice README без override — соответствует решению WP. Поля соответствуют `governance/architecture/metamodel/metadata.md` (`last_verified`, `versions` — необязательные override-поля). |
| C-4 — ссылки Pydantic | `closed` | Обе ссылки переведены на `https://pydantic.dev/docs/validation/latest/concepts/fields/` и `…/serialization/`; обе возвращают HTTP 200 (проверено curl). Старый домен `docs.pydantic.dev` в scope не находится. |
| C-5 — кэширование POST | `closed` | `project-spec/…:180-182`: сценарий 8 теперь про «два формата явно кэшируемого ответа»; добавлено, что ответ POST должен быть явно разрешён кэшированию на условиях RFC 9110 §9.3.3 и что общий кэш не сохраняет произвольный ответ POST по умолчанию; предложена альтернатива `GET /orders/{id}`. Перепроверено по RFC 9110 §9.3.3: "Responses to POST requests are only cacheable when they include explicit freshness information…" — формулировка корректна. |
| C-6 — сокращённый validation response | `closed` | `learn/http-contract-execution-boundary.md:33-45`: перед примером прямо названо «сокращённый вариант с полями, важными для разбора», добавлено `"msg": "Field required"`. Опущенные `input`/`url` не перечислены, но WP требовал перечисление только в альтернативной ветке («либо»); добавление `msg` удовлетворяет основной ветке. |
| C-7 — терминология участников | `closed` | `interview/…:46` и `project-spec/…:59` при первом употреблении дают русское объяснение `consumer`/`producer`/`provider` и ссылку на раздел brief с `API caller`/`API provider`; словари обновлены (`interview/…:261-262`, `project-spec/…:340`). Якорь `#какие-комбинации-версий-нужно-проверить` соответствует существующему заголовку `learn/compatible-api-change.md:211`. Распространённые термины не запрещены — соответствует WP. |
| C-8 — формат JSON Pointer | `closed` | `learn/http-contract-execution-boundary.md:156`: `"pointer": "#/delivery/window/end"` — fragment-форма, совпадающая с примером RFC 9457 §3 (`#/age`, `#/profile/color`). Выбран предпочтительный вариант WP. |
| C-9 — диапазон уровней interview | `closed` | `curriculum/tracks/b2/coverage-map.md:41`: «L2–L4 follow-ups по design/evolution/compatibility» — соответствует фактическим targets (в coverage interview нет `-l1-`). Остальные строки coverage-map не изменены; поиск по `L1–L4 follow-ups` пуст. |
| C-10 — response model include/exclude | `closed` | `learn/http-contract-execution-boundary.md:196`: утвердительная форма — «Фактический ответ фильтруется, а сгенерированная JSON Schema продолжает описывать полную модель». Перепроверено по FastAPI Response Model docs: "the JSON Schema generated in your app's OpenAPI (and the docs) will still be the one for the complete model, even if you use response_model_include or response_model_exclude". Практический вывод (отдельная выходная модель) сохранён. |
| C-11 — default dependency scope | `closed` | `learn/http-contract-execution-boundary.md:104`: явно указано, что для зависимости с `yield` scope по умолчанию — `request` (завершающий код после отправки ответа), а `scope="function"` переносит завершение после возврата операции, но до отправки ответа. Совпадает с FastAPI Dependencies with yield docs ("If not specified and the dependency has yield, it will have a scope of request by default"). В источники добавлена прямая ссылка на эту страницу. |

Итого: **11 closed, 0 partially_closed, 0 not_closed, 0 not_applicable.**

## Regression checks

- **Структура:** ID, `owner_track`, `module`, `status: review`, coverage targets и roles во всех шести файлах не изменились; механический пересчёт — 43 coverage edges / 21 unique target, как до правок и как в G4.
- **Технические формулировки:** новые фрагменты о кэше (оба briefs, project-spec сценарий 8) нормативно точны; ложной категоричности не появилось — требования кэша приписаны RFC 9111, определение `Vary` — RFC 9110.
- **Ссылки:** относительные ссылки README и новые кросс-ссылки interview/project-spec на brief разрешаются; все новые внешние URL возвращают 200; формат якорей rfc-editor (`#section-…`) соответствует разметке RFC.
- **Markdown/Mermaid/`<details>`:** все шесть файлов сбалансированы (скрипт: парность fences, `<details>`/`<summary>`); conflict markers и trailing whitespace отсутствуют.
- **Примеры одной истории:** во всех документах единые `customer_id: "c-42"`, `product_id: "p-7"`, `{"error": "invalid order"}`, правила `pickup`/`scheduled`; противоречий не осталось.
- **Readability:** вставки — связная проза с русским объяснением перед английским термином (соответствует `authoring/standards/editorial-standard.md` §4); терминологические абзацы в interview/project-spec стоят до первого использования терминов; телеграфный стиль и машинная нотация не вернулись.
- **Scope:** по mtime окно правок автора (17:44) затронуло ровно пять материалов слайса + `coverage-map.md` (строка C-9); slice README без изменений, что согласуется с C-3. Governance-файлы (`governance/state/STATUS.md`, `governance/decisions.md`, `governance/planning/execution-plan.md`, `README.md`, `docs/10`, оба work packages) изменялись в отдельных окнах 17:26 и 19:02 — до и после контентных правок, что соответствует ролям оркестратора (dispatch/intake). Каталог не является git-репозиторием, поэтому diff-scope верифицирован косвенно (mtime + чтение), а не по VCS — это ограничение данной проверки, а не finding против пакета.
- **Учебный замысел:** готовое решение kata/project не добавлено; уровни L2/L3, сложность interview и последовательность обучения не изменены; слайс не расширен в справочник (вставки локальны: один абзац в briefs, один абзац в interview/project-spec, одно уточнение сценария).

## Новые findings

Нет. Изменённые фрагменты не содержат новых технических, структурных или редакционных проблем.

## Counts

| Severity | Новых findings |
|---|---|
| blocker | 0 |
| major | 0 |
| minor | 0 |
| note | 0 |

## Primary sources, использованные для перепроверки

- [RFC 9110 — HTTP Semantics](https://www.rfc-editor.org/rfc/rfc9110.html) — §9.3.3 (кэшируемость POST), §12.5.5 (Vary);
- [RFC 9111 — HTTP Caching](https://www.rfc-editor.org/rfc/rfc9111.html) — §4.1 (Calculating Cache Keys with the Vary Header Field);
- [RFC 9457 — Problem Details for HTTP APIs](https://www.rfc-editor.org/rfc/rfc9457.html) — §3 (fragment-форма pointer в примере `errors`);
- [FastAPI: Response Model](https://fastapi.tiangolo.com/tutorial/response-model/) — поведение `response_model_include/exclude` со схемой полной модели;
- [FastAPI: Dependencies with yield](https://fastapi.tiangolo.com/tutorial/dependencies/dependencies-with-yield/) — default scope `request`, семантика `scope="function"`;
- [Pydantic docs (validation)](https://pydantic.dev/docs/validation/latest/concepts/fields/) — доступность канонических URL (HTTP 200).

## Verdict

**`accepted`.** Все C-1–C-11 закрыты корректными и достаточными правками; регрессий и новых blocker/major/minor не обнаружено; нормативные формулировки подтверждены первичными источниками на 2026-08-15.

Reviewer не менял статусы материалов и не объявляет M1.5/M1.6 завершёнными: acceptance и обновление milestone остаются ответственностью оркестратора.
